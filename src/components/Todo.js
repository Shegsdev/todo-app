import TodoList from "../containers/TodoList";
import TodoFilters from "./TodoFilters";

const Todo = () => {
  return (
    <div>
      <TodoList />
      <TodoFilters />
    </div>
  );
};

export default Todo;